import { Vector } from '../libs';
import { add, multiplyScalar, unit, isEqual } from '../libs/vector';


const a = new Vector(1, 2, 3);
const b = new Vector(10, 20, 30);
const newA = multiplyScalar(a, 10);

console.log(1/2);
